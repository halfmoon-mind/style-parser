name: style_parser
description: "this is a package to parse css to text style, so you can use css to style your text in flutter."
version: 0.0.13
homepage: https://github.com/halfmoon-mind/style-parser
repository: https://github.com/halfmoon-mind/style-parser

environment:
  sdk: ">=3.4.1 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  html: ^0.15.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
